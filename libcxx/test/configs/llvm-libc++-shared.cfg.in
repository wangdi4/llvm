# This testing configuration handles running the test suite against LLVM's libc++
# using a shared library.

lit_config.load_config(config, '@CMAKE_CURRENT_BINARY_DIR@/cmake-bridge.cfg')

config.substitutions.append(('%{flags}',
    '-pthread' + (' -isysroot {}'.format('@CMAKE_OSX_SYSROOT@') if '@CMAKE_OSX_SYSROOT@' else '')
))
# INTEL_CUSTOMIZATION
# CMPLRLLVM-47181: -fno-assume-sane-operator-new was added because the lit
# tests overload the operator new. The overloaded operator can modify a global,
# but the compiler treats new as it won't modify a value. It is assuming that
# new is conformant with the C++ standards.
config.substitutions.append(('%{compile_flags}',
    '-nostdinc++ -fno-assume-sane-operator-new -I %{include} -I %{target-include} -I %{libcxx}/test/support'
))
# end INTEL_CUSTOMIZATION
config.substitutions.append(('%{link_flags}',
    '-nostdlib++ -L %{lib} -Wl,-rpath,%{lib} -lc++'
))
config.substitutions.append(('%{exec}',
    '%{executor} --execdir %T -- '
))

import os, site
site.addsitedir(os.path.join('@LIBCXX_SOURCE_DIR@', 'utils'))
import libcxx.test.params, libcxx.test.config
libcxx.test.config.configure(
    libcxx.test.params.DEFAULT_PARAMETERS,
    libcxx.test.features.DEFAULT_FEATURES,
    config,
    lit_config
)
