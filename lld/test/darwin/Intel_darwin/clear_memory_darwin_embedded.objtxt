# REQUIRES: asserts

# RUN: ld64.lld -arch x86_64 -lto_library %t -intel-debug=mem -intel-embedded-linker -r %s 2>&1 | FileCheck %s
#
# This test checks that the memory wasn't freed when running
# lld without early exit.
#

# CHECK: ld64.lld: warning: Cleaning up LLD memory
# CHECK-NOT: ld64.lld: warning: Cleaning up LLVM memory

--- !native
defined-atoms:
    - name:              _foo
...
