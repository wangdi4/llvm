if (WIN32)
	if (CMAKE_SIZEOF_VOID_P EQUAL 4)
	  create_target (corei7-avx g9 )	
	else ()
	  create_target (corei7-avx e9 )
	endif(CMAKE_SIZEOF_VOID_P EQUAL 4)
else (WIN32)
	if (CMAKE_SIZEOF_VOID_P EQUAL 4)
	  create_target (corei7-avx g9 )	
	else ()
	  create_target (corei7-avx e9 )
	endif(CMAKE_SIZEOF_VOID_P EQUAL 4)
endif (WIN32)
