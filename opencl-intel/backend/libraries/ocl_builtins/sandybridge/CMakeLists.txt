if (WIN32)
	if (CMAKE_SIZEOF_VOID_P EQUAL 4)
	  create_target (corei7-avx g9 TRUE )
	else ()
	  create_target (corei7-avx e9 TRUE )
	endif(CMAKE_SIZEOF_VOID_P EQUAL 4)
else (WIN32)
	if (CMAKE_SIZEOF_VOID_P EQUAL 4)
	  create_target (corei7-avx g9 TRUE )
	else ()
	  create_target (corei7-avx e9 TRUE )
	endif(CMAKE_SIZEOF_VOID_P EQUAL 4)
endif (WIN32)
