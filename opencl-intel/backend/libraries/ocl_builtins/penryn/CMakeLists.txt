if (WIN32)
	if (CMAKE_SIZEOF_VOID_P EQUAL 4)
	  create_target ( penryn p8 )
	else ()
	  create_target ( penryn y8 )
	endif(CMAKE_SIZEOF_VOID_P EQUAL 4)
else (WIN32)
	if (CMAKE_SIZEOF_VOID_P EQUAL 4)
	  create_target ( penryn p8 )
	else ()
	  create_target ( penryn y8 )
	endif(CMAKE_SIZEOF_VOID_P EQUAL 4)
endif (WIN32)