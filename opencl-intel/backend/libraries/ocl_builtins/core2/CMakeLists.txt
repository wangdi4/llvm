if (WIN32)
	if (CMAKE_SIZEOF_VOID_P EQUAL 4)
	  create_target ( core2 v8 TRUE )
	else ()
	  message (FATAL_ERROR "No support for Atom on Win64!")
	endif (CMAKE_SIZEOF_VOID_P EQUAL 4)
else (WIN32)
	if (CMAKE_SIZEOF_VOID_P EQUAL 4)
	  message (FATAL_ERROR "No yet support for Atom on Lin32!")
	else ()
	  message (FATAL_ERROR "No support for Atom on Lin64!")
	endif (CMAKE_SIZEOF_VOID_P EQUAL 4)
endif (WIN32)

