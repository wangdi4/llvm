__kernel void
invert_colors (	__global const float4 *a,
		__global float4 *b)
{
	int gid = get_global_id(0);
	b[gid].x = 1 - a[gid].x;
	b[gid].y = 1 - a[gid].y;
	b[gid].z = 1 - a[gid].z;
	b[gid].w = a[gid].w;
}
