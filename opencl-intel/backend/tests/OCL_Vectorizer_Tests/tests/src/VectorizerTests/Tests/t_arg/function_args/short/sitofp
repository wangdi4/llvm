/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM sitofp instruction when it recieves short function argument
 * as input and turns it into float.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
sitofp (	__global const float *in,
		__global float *out,  const short shortArg1)
{
	int gid = get_global_id(0);
	out[gid] = shortArg1;
}
