/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL load instruction when it recieves double function argument
 * as input.
 * This function is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
load_double_ptr
( __global const double *in,
		__global double *out)
{
	int gid = get_global_id(0);
	out[gid] = *in;
}
