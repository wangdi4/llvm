/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM trunc instruction when it recieves ushort function argument
 * as input and turns it into uchar.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
trunc_ushort (	__global const float *in,
		__global float *out,  const ushort ushortArg1)
{
	int gid = get_global_id(0);
	uchar tmp = ushortArg1;
	out[gid] = tmp;
}
