/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL mad_hi function when it recieves uint function arguments
 * as input.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_mad_hi
( __global const float *in,
		__global float *out,  const uint uintArg1,  const uint uintArg2,  const uint uintArg3)
{
	int gid = get_global_id(0);
	out[gid] = mad_hi(uintArg1, uintArg2, uintArg3);
}
