__kernel void
atom_add07
( __global  int *in,
		__global float *out)
{
	int gid = get_global_id(0);
	in[gid]=gid;
	
	int rez=atom_add(&in[gid],gid)+atom_add(&in[gid],gid);
	
	
	out[gid] = rez+in[gid];
}
