/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM NON vectorizable upsample function when it receives as input 2 uchar
 * arguments produced by previous NON vectorizable min function.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_min_func_upsample ( __global const float *in,
		__global float8 *out)
{
	int gid = get_global_id(0);
	uchar arg1 = in[gid];
	uchar arg2 = in[gid] * in[gid];
	uchar arg3 = in[gid] + in[gid];
	uchar arg4 = in[gid] + 43;
	uchar arg5 = min(arg1, arg2);
	uchar arg6 = min(arg3, arg4);
	short arg7 = upsample(arg5, arg6);
	out[gid].s0 = arg1;
	out[gid].s1 = arg2;
	out[gid].s2 = arg3;
	out[gid].s3 = arg4;
	out[gid].s4 = arg5;
	out[gid].s5 = arg6;
	out[gid].s6 = arg7;
}
