/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL ptrtoint instruction when it recieves uchar* function argument
 * as input and turns it into int.
 * This function is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
ptrtoint
( __global const uchar2 *in,
		__global float *out)
{
	int gid = get_global_id(0);
	int tmp = (*in).s1;
	tmp = tmp * 2;
	out[gid] = tmp;
}
