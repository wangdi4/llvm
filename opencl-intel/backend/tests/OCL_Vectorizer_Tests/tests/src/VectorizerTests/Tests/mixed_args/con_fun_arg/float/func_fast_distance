/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL fast_distance function when it recieves float constant
 * and float function argument as input.
 * This function is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_fast_distance
( __global const float *in,
		__global float *out,  const float floatArg1)
{
	int gid = get_global_id(0);
	out[gid] = fast_distance(floatArg1, FLOAT1);
}
