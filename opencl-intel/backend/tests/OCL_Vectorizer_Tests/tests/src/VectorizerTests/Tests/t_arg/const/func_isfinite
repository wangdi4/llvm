/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL isfinite function when it recieves float16 constants.
 * This function is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_isfinite
( __global const float *in,
		__global float16 *out)
{
	int gid = get_global_id(0);
	out[gid] = convert_float16(isfinite(FLOAT16_VEC1));
}
