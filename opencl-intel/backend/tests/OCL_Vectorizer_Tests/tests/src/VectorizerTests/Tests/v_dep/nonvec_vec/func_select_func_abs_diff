/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM vectorizable abs_diff function when it receives as input 2 uchar 
 * arguments produced by previous NON vectorizable select function.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_select_func_abs_diff ( __global const float *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	uchar gidUchar = gid;
	uchar inGidUchar = in[gid];
	uchar tmp = inGidUchar * inGidUchar;
	uchar arg1 = select(gidUchar, inGidUchar, tmp);
	uchar arg2 = select(inGidUchar, tmp, gidUchar);
	uchar arg3 = abs_diff(arg1, arg2);
	out[gid].x = arg1;
	out[gid].y = arg2;
	out[gid].z = arg3;
}
