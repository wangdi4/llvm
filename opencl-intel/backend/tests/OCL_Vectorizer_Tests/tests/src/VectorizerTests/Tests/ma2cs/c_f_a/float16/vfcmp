/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM vfcmp instruction when it recieves float16 constant and recieves
 * float16 function argument as input.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
vfcmp ( __global const float *in,
		__global float16 *out,  const float16 float16Arg1)
{
	int gid = get_global_id(0);
	out[gid] = as_float16(float16Arg1 == FLOAT16_VEC1);
}
