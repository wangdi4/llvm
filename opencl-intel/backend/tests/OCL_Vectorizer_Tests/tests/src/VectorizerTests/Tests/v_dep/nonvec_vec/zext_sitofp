/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM vectorizable sitofp instuction when it receives as input long argument
 * produced by previous NON vectorizable zext instruction (works on ushort).
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
zext_sitofp ( __global const float *in,
		__global float2 *out)
{
	int gid = get_global_id(0);
	ushort inGidUshort = in[gid];
	long arg1 = inGidUshort;
	out[gid].x = inGidUshort;
	out[gid].y = arg1;
}
