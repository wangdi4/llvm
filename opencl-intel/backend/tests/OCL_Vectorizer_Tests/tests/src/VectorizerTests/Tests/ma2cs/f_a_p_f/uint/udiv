/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM div instruction when it recieves uint argument produced by previous instructions
 * and recieves uint function argument as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
udiv ( __global const float *in,
		__global float *out,  const uint uintArg1)
{
	int gid = get_global_id(0);
	uint tmp = max((float)1, (float)(gid + in[gid]));
	out[gid] = convert_float(uintArg1 / tmp);
}
