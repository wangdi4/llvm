/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM NON vectorizable length function when it receives as input double argument
 * produced by previous vectorizable erfc function.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_erfc_func_length ( __global const float *in,
		__global float8 *out)
{
	int gid = get_global_id(0);
	double arg1 = in[gid];
	double arg2 = erfc(arg1);
	double arg3 = length(arg2);
	float2 arg4 = as_float2(arg1);
	float2 arg5 = as_float2(arg2);
	float2 arg6 = as_float2(arg3);
	out[gid].s0 = arg4.x;
	out[gid].s1 = arg4.y;
	out[gid].s2 = arg5.x;
	out[gid].s3 = arg5.y;
	out[gid].s4 = arg6.x;
	out[gid].s5 = arg6.y;
}
