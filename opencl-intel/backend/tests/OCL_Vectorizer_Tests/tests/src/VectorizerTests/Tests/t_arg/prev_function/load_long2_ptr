/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM load instruction when it recieves as input int produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
load_long2_ptr (	__global const long2 *in,
		__global long2 *out)
{
	int gid = get_global_id(0);
	long2* tmp1 = (long2*)in;
	long2* tmp2 = tmp1 + gid;
	long2* tmp3 = tmp1 + (gid / 3);
	long2* tmp4 = tmp3 > tmp2 ? tmp2 : tmp3;
	out[gid] = *tmp4;
}
