/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM shufflevector instruction when it recieves long8 function
 * arguments.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
shufflevector ( __global const float *in,
		__global float16 *out,  const long8 long8Arg1)
{
	int gid = get_global_id(0);
	long8 tmp = long8Arg1 + long8Arg1;
	tmp.s4 = long8Arg1.s0;
	out[gid] = as_float16(tmp);
}
