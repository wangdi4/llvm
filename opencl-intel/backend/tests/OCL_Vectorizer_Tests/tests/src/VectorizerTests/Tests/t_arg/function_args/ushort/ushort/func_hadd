/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL hadd function when it recieves short function argument
 * as input.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_hadd
( __global const float *in,
		__global float *out,  const ushort ushortArg1,  const ushort ushortArg2)
{
	int gid = get_global_id(0);
	out[gid] = hadd(ushortArg1, ushortArg2);
}
