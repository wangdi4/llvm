/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fptosi instruction when it recieves double function argument
 * as input and turns it into long.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fptosi (	__global const float *in,
		__global float *out, const double doubleArg1)
{
	int gid = get_global_id(0);
	out[gid] = (long) doubleArg1;
}
