 /*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM islessequal function when it recieves float8 constant and recieves
 * float8 function argument as input.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *old name func_isunordered
 */

__kernel void
f_isun ( __global const float *in,
		__global float8 *out,  const float8 float8Arg1)
{
	int gid = get_global_id(0);
	out[gid] = convert_float8(isunordered(float8Arg1, FLOAT8_VEC1));
}
