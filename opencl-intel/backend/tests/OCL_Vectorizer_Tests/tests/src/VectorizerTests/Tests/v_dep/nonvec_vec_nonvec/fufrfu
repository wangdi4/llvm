/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM non vectorizable upsample function appearing after the vectorizable rotate function when
 * they both receive as input uint8 arguments produced by previous NON vectorizable
 * upsample function.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fufrfu ( __global const float8 *in,
		__global float16 *out)
{
	int gid = get_global_id(0);
	int8 arg1 = as_int8(in[gid]);
	int8 arg2 = arg1 + arg1;
	uint8 arg3 = abs(arg1);
	uint8 arg4 = abs(arg2);
	uint8 arg5 = rotate(arg3, arg4);
	ulong8 arg6 = upsample(arg3, arg4);
	out[gid] = as_float16(arg6);

}
