/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM cross function when it recieves float function argument as input
 *  and recieves as input float argument produced by previous instructions.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_cross ( __global const float *in,
		__global float4 *out,  const float4 floatArg1)
{
	int gid = get_global_id(0);
	float4 tmp = (float4)(in[gid] + gid / 1644564, in[gid], gid / 3.2012 + in[gid], gid);
	out[gid] = cross(floatArg1, tmp);
}
