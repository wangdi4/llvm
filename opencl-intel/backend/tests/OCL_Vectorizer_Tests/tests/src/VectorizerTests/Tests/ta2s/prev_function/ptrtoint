/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM ptrtoint instruction when it recieves as input long16 produced by
 * previous instruction and turns it into uchar and called twice.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
ptrtoint (	__global const long4 *in,
		__global float2 *out)
{
    int *p=0;
	int gid = get_global_id(0);
	long4* tmp1 = (long4*)(p);
	tmp1 = tmp1 + convert_int(gid/ 2.489469);
	uchar tmp2 = tmp1;
	tmp2 = tmp2 + 17;
	uchar tmp3 = tmp1;
//	tmp3 = tmp3 * tmp2;
	tmp3 = tmp3 +  tmp1;
	out[gid].x = tmp2;
	out[gid].y = tmp3;
}
