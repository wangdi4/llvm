/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM udiv instruction when it recieves uint function arguments
 * as input.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
udiv (	__global const float *in,
		__global float *out,  const uint uintArg1,  const uint uintArg2)
{
	int gid = get_global_id(0);
	out[gid] = uintArg2 / uintArg2;
}
