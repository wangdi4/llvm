/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM NON vectorizable sext instrictoion appearing after the vectorizable
 * mul instruction when it receives as input short argument produced by previous
 * (NON vectorizable) trunc instruction that receives constants and function arguments
 * as input and therfore will have scalar value.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
trunc_mul_sext ( __global const float *in,
		__global float *out, const int intArg1,  const int intArg2)
{
	int gid = get_global_id(0);
	short arg1 = intArg1;
	short arg3 = arg1 * arg1;
	int arg4 = arg3;
	out[gid] = convert_float(arg4);
}

