/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fptrunc instruction when it recieves as input float produced by
 * previous instruction and turns it into ushort.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fptoui ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	int index = max (gid - 1, 0);
	ushort temp = distance(in[gid], in[index]);
	out[gid] = temp;
}
