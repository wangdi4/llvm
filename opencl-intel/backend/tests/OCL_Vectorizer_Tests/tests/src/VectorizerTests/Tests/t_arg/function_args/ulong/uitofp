/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM uitofp instruction when it recieves ulong function argument
 * as input and turns it into double.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
uitofp (	__global const float *in,
		__global float *out,  const ulong ulongArg1)
{
	int gid = get_global_id(0);
	double tmp = ulongArg1;
	out[gid] = tmp;
}
