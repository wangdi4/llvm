/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM ashr instruction when it recieves char4 constant.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
ashr ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	long4 vec = (ARG1, ARG1, ARG1, ARG1);
	long4 res = LONG4_VEC1 >> vec;
	out[gid] =  res.x & res.y & res.z & res.w;
}
