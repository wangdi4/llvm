/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fcmp instruction when it recieves float4 constant.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fcmp (	__global const float4 *in,
		__global float *out)
{
	int gid = get_global_id(0);
	int4 res = (FLOAT4_VEC1 == ARG0);
	out[gid] = res.x + res.y + res.z + res.w;

}
