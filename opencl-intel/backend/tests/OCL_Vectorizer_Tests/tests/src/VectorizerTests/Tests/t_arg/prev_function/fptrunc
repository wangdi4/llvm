/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fptrunc instruction when it recieves as input double produced by
 * previous instruction.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fptrunc ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	double temp = ceil(in[gid]);
	// these mul and add are done to make sure that fptrunc will be used (on double)
	temp = temp * in[gid];
	temp = temp + 23;
	out[gid] = temp;
}
