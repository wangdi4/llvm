/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL bitselect function when it recieves ulong constants.
 * This function is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_bitselect
( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	out[gid] = bitselect(ULONG1, ULONG2, ULONG3);
}
