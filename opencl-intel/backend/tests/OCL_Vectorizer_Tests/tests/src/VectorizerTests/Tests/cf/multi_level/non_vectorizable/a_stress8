__kernel void a_stress8 ( __global const long3 *in,
		__global float *out)
{	
	int i,j=0;
	float sum=0;
	int gid = get_global_id(0);
	
	long k=12000;
	double rez=1;
	
	//printf("\n hello \n");
	
	for (i=1;i<k;i++)
	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
	
		{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}	{
	
		while (j<k+k)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
		rez=rez+k+in[gid].z;
	}	
	
	
	
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			rez=rez+1+i*k+in[gid].x-in[gid].y;
		}
	}
	
	j=12;
	for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1;
			rez=rez+k+in[gid].z;
		}
		
		sum=sum+0.3;
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
		for (i=1;i<k+j;i++)
	{
	
		while (j<k*7)
		{
		
			j=j+i;
			sum=sum+1/(rez);
		}
		rez=rez+1+i*k+in[gid].x-in[gid].y;
	}
	
	//printf("\n i=%d gid=%d j=%d sum=%f\n",i,gid,j,sum);
	out[gid] = i* gid + j-  sum;
	
	
	
	

}