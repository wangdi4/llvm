/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM select instruction when it recieves float function arguments.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
select_float ( __global const float2 *in,
		__global float *out,  const float floatArg1,  const float floatArg2,  const float floatArg3)
{
	int gid = get_global_id(0);
	out[gid] =(int) floatArg1 ? floatArg2 : floatArg3;
}
