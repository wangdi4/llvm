/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM bitcast instruction when it recieves double4 function argument as input
 * and turns it into ushort16.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
bitcast (	__global const float *in,
		__global float8 *out,  const double4 double4Arg1)
{
	int gid = get_global_id(0);
	ushort16 tmp = as_ushort16(double4Arg1);
	// this add is done to make sure that tmp is created
	tmp = tmp + tmp;
	out[gid] = as_float8(tmp);
}
