/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fpext instruction when it recieves float function argument
 * as input.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fpext ( __global const float *in,
		__global float *out,  const float floatArg1)
{
	int gid = get_global_id(0);
	// this operations are done to make sure that double tmp is created and fpext
	// is used
	double tmp = 3;
	tmp = tmp * floatArg1;
	tmp = tmp + floatArg1;
	out[gid] = tmp;
}
