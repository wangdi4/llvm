/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL mul_hi function when it recieves char function arguments
 * as input and called twice.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
fmh ( __global const float *in,
		__global float2 *out,  const char charArg1,  const char charArg2)
{
	int gid = get_global_id(0);
	char res1 = mul_hi(charArg1, charArg2);
	char res2 = mul_hi(charArg1, charArg2);
	out[gid].x = res1;
	out[gid].y = res2;
}
