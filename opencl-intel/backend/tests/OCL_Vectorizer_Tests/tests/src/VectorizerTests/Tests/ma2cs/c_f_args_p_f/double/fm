/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL mix function when it recieves double constant
 * and double argument produced by previous instructions and double
 * function argument as input.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *func_mix
 */

__kernel void
fm
( __global const float *in,
		__global float *out,  const double doubleArg1)
{
	int gid = get_global_id(0);
	double tmp = in[gid] - gid * 1.46564 + 12.45646;
	out[gid] = convert_float(mix(doubleArg1, tmp, DOUBLE1));
}
