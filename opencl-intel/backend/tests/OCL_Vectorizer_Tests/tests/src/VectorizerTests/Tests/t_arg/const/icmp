/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM icmp instruction when it recieves ushort constant.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
icmp (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	out[gid] = (USHORT1 == ((ushort) ARG0));
}
