/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM insertelement instruction when it recieves int function argument as input
 * and recieves as input int16 argument produced by previous instructions and recieves
 * int const (the position on insretelement).
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
insertelement( __global const float *in,
		__global float16 *out,  const int intArg1)
{
	int gid = get_global_id(0);
	int x = gid;
	int y = in[gid] - gid * gid + (in[gid] * 4);
	int z = x - y + gid;
	int w = gid - 5 + z;
	int16 tmp = (int16)(w , z, y, x, x + y, x - z, w * x, w * y  - ((w + x) * 2 + 1), w + 17.39203165, y - z * w, x, z -y, w, z, z, x + z);
	tmp = tmp * tmp;
	tmp.x = intArg1;
	out[gid] = convert_float16(tmp);
}
