// Tests bitcast instruction via WIA.
// The 'bitcast' instruction converts value to type ty2 without changing any bits.






__kernel void wia_27_bitcast(__global int *in,__global float8 *out,const ptrdiff_t intPtrArg1)
{
	
	double4 double4Arg1=(double4) (1,1,1,1); //uniform
	ushort16 tmp = as_ushort16(double4Arg1); //uniform
	int gid = get_global_id(0);
	int i;
	for (i=1;i<256;i++)  
	{
		double4Arg1+=1; //unif
		tmp = as_ushort16(double4Arg1); //uniform
		tmp=tmp+tmp+tmp;
		out[gid].s0+=(float)tmp.s0;	
	}
	
	tmp = tmp + tmp;
	out[gid] = as_float8(tmp);

}