/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL rootn function when it recieves float constant
 * and called twice.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_rootn ( __global const float *in,
		__global float2 *out)
{
	int gid = get_global_id(0);
	float res1 = rootn(FLOAT1, INT1);
	float res2 = rootn(FLOAT1, INT1);
	out[gid].x = res1;
	out[gid].y = res2;
}
