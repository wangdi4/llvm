/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL min function when it recieves short constants
 * and called trwice.
 * This function is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_min
( __global const float *in,
		__global float2 *out)
{
	int gid = get_global_id(0);
	short res1 = min(SHORT1, SHORT2);
	ulong res2 = min(SHORT1, SHORT2);
	out[gid].x = res1;
	out[gid].y = res2;
}
