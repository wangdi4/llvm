/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL step function when it recieves double constant
 * and double argument produced by previous instructions.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_step
( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	double tmp = gid - in[gid] / max((double)(gid * in[gid]), (double)(1.529612));
	out[gid] = step(tmp, DOUBLE1);
}
