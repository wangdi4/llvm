/*
 * Part of the control flow tests that check vectorization of control flow statements.
 *
 * Tests the LLVM br caused by goto statement when it doesn't depend on thread id.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
goto_test (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);

	goto label1;
	out[gid] = 1;

	label1:
	out[gid] = 17;
}
