/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM mul instruction when it recieves int constant and recieves as
 * input int argument produced by previous instructions.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
mul ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	int tmp = gid + in[gid];
	tmp = min(tmp, (int)(in[gid] * in[gid] - 5));
	out[gid] = INT1 * tmp;
}
