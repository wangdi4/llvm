/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM shffelevector instruction when it recieves uint8 constant
 * and recieves as input uint8 argument produced by previous instructions.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
shffelevector ( __global const float *in,
		__global float8 *out)
{
	int gid = get_global_id(0);
	uint x = gid + 5 - exp(in[gid]);
	uint y = x - 5 * 17 + 2886;
	uint z = max(x, y) -x * y;
	uint8 tmp = (uint8)(x, y, z, x + y - z, x * z, z + x, x, y);
	tmp.s0273546 = UINT8_VEC1.s0543261;
	out[gid] = convert_float8(tmp);
}
