/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM insertelement instruction when it recieves int4 constant
 * and int index argument produced by previous instructions and int function argument to insert to the vector as input.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
store ( __global const float *in,
		__global int *out)
{
	int gid = get_global_id(0);
	int tmp = max(gid, (gid / 3) + 17);
	out[tmp] = INT1;

}
