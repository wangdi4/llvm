/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL select instruction when it recieves float constant
 * and double argument produced by previous instructions and double
 * function argument as input.
 * This function is vectorizable.
 *
 * @param in
 *     The input array.
 * @param out
 *     The output array.
 *
 */

__kernel void
select_float
( __global const float *in,
    __global float *out, __global const float floatArg1)
{
  int gid = get_global_id(0);
  float tmp =  remainder(gid, in[gid]) * in[gid] - 434.46456;
  out[gid] = tmp ? FLOAT1 : floatArg1;
}
