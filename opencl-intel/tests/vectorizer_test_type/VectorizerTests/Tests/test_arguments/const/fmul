/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fmul instruction when it recieves float constant.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
fmul ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	out[gid] = FLOAT1 * ARG1;
}
