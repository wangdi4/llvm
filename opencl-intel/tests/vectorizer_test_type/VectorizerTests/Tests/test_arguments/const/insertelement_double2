/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM insertelement instruction when it recieves double2 constant.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
insertelement_double2 ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	double2 vec = DOUBLE2_VEC1;
	// these assignment and add are done to make sure that insertelement will be used (on vec)
	vec.x = ARG0;
	vec = vec + vec;
	out[gid] = vec.x + vec.y;
}
