/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM bitcast instruction when it recieves char8 constant input argument
 * and turns it into long.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
bitcast_char8 ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	out[gid] = as_long(CHAR8_VEC1);
}
