/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM udiv instruction when it recieves uchar8 constant.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

// TODO : this test is commented out beacuse of apple's bug, waiting for fix

__kernel void
udiv ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
//	uchar8 vec = (ARG1, ARG1, ARG1, ARG1, ARG1, ARG1, ARG1, ARG1);
//	uchar8 res = vec / UCHAR8_VEC1;
//	out[gid] = res.s0 + res.s1 + res.s2 + res.s3 + res.s4 + res.s5 + res.s6 + res.s7;
}
