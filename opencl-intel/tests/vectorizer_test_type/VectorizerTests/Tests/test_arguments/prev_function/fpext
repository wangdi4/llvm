/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fpext instruction when it recieves as input float produced by
 * previous instruction and turns it into double.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fpext ( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	float tmp = dot(in[gid], (float)(gid * (erfc(in[gid] * in[gid]))));
	double tmp2 = tmp;
	tmp = tmp * 4;
	tmp2 = tmp2 * tmp;
	tmp2 = tmp2 / 3;
	out[gid] = tmp2;
}
