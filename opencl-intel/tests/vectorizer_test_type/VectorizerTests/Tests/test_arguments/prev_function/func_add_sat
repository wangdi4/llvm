/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM add_sat function when it recieves as input int produced by
 * previous instruction.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_add_sat (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	int tmp1 = convert_ushort(convert_int(in[gid] + radians(in[gid])));
	int tmp2 = clz(tmp1);
	out[gid] = add_sat(tmp1, tmp2);
}
