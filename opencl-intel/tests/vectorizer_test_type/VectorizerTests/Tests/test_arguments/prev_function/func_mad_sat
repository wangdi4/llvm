/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM mad_sat function when it recieves as input ushort produced by
 * previous instruction.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_mad_sat (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	ushort tmp1 = convert_ushort(lgamma(in[gid] + in[gid] / 7));
	ushort tmp2 = (ushort) cbrt(in[gid] * 10);
	ushort2 tmp3 = as_ushort2(in[gid]);
	ushort tmp4 = tmp3.x;
	out[gid] = mad_sat(tmp1, tmp2, tmp4);
}
