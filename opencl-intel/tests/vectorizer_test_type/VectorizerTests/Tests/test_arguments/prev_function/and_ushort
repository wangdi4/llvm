/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM and instruction when it recieves as input ushort arguments
 * produced by previous instructions.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
and_ushort (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	ushort temp1 = in[gid] * ARG1;
	ushort temp2 = in[gid] * in[gid];
	out[gid] = temp1 & temp2;
}
