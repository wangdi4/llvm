/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM zext instruction when it recieves as input uchar produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
zext (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	uchar temp1 = min(ARG0, in[gid]);
	// the conversion to short should use zext
	short temp2 = temp1 + 3;
	out[gid] = temp2;
}
