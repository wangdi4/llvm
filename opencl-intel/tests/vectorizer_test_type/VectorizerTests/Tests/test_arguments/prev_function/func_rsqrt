/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM rsqrt function when it recieves as input float produced by
 * previous instruction.
 * This instruction is vectorizable.
 *
 * @param in
 *     The input array.
 * @param out
 *     The output array.
 *
 */
__kernel void
func_rsqrt (  __global const float *in,
    __global float *out)
{
  int gid = get_global_id(0);
  out[gid] = rsqrt(normalize(in[gid]) + cos(in[gid] * 2));
}
