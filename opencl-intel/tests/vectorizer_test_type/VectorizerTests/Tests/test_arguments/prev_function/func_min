/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM min function when it recieves as input uint4 produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_min (	__global const float *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	uint x = convert_uint(in[gid]);
	uint y = clz(x) + x;
	uint z = abs((int)(y - x));
	uint w = abs((int)(y * z - x));
	uint4 tmp1 = (uint4)(x, y * z, y + w, w - x * y);
	uint4 tmp2 = (uint4)( y, x * w * z, w + 17, (uint) (z /(x + 1)));
	out[gid] = convert_float4(min(tmp1, tmp2));
}
