/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM mul_hi function when it recieves as input long produced by
 * previous instruction.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_mul_hi (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	long tmp1 = (long)(gid * in[gid] / 9);
	long tmp2 = convert_long(in[gid] + gid * distance(in[gid], in[gid] * 25));
	out[gid] = mul_hi(tmp1, tmp2);
}
