/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM store instruction when it recieves float function argument
 * as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
store_float (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	out[gid] = in[gid];
}
