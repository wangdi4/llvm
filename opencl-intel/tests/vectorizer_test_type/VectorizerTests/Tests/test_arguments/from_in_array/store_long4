/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM store instruction when it recieves long4 function argument
 * as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
store_long4 (	__global const float8 *in,
		__global long4 *out)
{
	int gid = get_global_id(0);
	out[gid] = as_long4(in[gid]);
}
