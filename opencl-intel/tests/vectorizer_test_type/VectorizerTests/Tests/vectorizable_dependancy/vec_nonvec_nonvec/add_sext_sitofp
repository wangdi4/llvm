/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM NON vectorizable sitofp instruction appearing after the NON vectorizable sext instruction when
 * they both receive as input char arguments produced by previous vectorizable
 * icmp instruction.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
add_sext_sitofp ( __global const float *in,
		__global float2 *out)
{
	int gid = get_global_id(0);
	char arg1 = gid;
	char arg2 = in[gid];
	char arg3 = arg1 + arg2;
	int arg4 = arg3;
	arg4 = arg4 + 5;
	float arg5 = arg3;
	out[gid].x = arg4;
	out[gid].y = arg5;
}
