/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM and instruction when it recieves uchar constant.
 * This instruction is vectorizable and called twice.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
and_uchar ( __global const float *in,
		__global float2 *out)
{
	int gid = get_global_id(0);
	uchar tmp = ((uchar)ARG1);
	uchar res1 = UCHAR1 & tmp;
	tmp = tmp + tmp;
	uchar res2 = UCHAR1 & tmp;
	out[gid].x = res1;
	out[gid].y = res2;
}
