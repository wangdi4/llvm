/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL select function when it recieves uint constants
 * and called trwice.
 * This function is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_select
( __global const float *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	ulong res1 = select(ULONG1, ULONG2, ULONG3);
	ulong res2 = select(ULONG1, ULONG2, ULONG3);
	float2 tmp1 = as_float2(res1);
	float2 tmp2 = as_float2(res2);
	out[gid].x = tmp1.x;
	out[gid].y = tmp1.y;
	out[gid].z = tmp2.x;
	out[gid].w = tmp2.y;
}
