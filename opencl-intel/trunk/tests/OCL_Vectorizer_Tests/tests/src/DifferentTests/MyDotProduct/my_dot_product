__kernel void
my_dot_product (	__global const float4 *a,
		__global const float4 *b, __global float *c)
{
	int gid = get_global_id(0);
	c[gid] = a[gid].x * b[gid].x + a[gid].y * b[gid].y + a[gid].z * b[gid].z + a[gid].w * b[gid].w;
}
