__kernel void
mul_vector (	__global const float4 *a,
		__global float4 *b)
{
	int gid = get_global_id(0);
	// using local vectorized variable (temp)
	float4 temp = a[gid] * (2.0f, 2.0f, 2.0f, 2.0f);
	b[gid] = temp * (3.0f, 3.0f, 3.0f, 3.0f);
}
