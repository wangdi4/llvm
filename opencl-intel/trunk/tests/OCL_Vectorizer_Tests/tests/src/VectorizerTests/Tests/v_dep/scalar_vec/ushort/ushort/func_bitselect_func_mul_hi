/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM vectorizable mul_hi function when it receives as input 3 ushort
 * arguments produced by previous (NON vectorizable) bitselect function that receives
 * constants and function arguments as input and therfore will have scalar value.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_bitselect_func_mul_hi ( __global const float *in,
		__global float4 *out,  const ushort ushortArg1,  const ushort ushortArg2)
{
	int gid = get_global_id(0);
	short arg1 = bitselect(USHORT1, ushortArg1, ushortArg2);
	short arg2 = bitselect(ushortArg1, ushortArg2, USHORT1);
	short arg3 = mul_hi(arg1, arg2);
	out[gid].x = arg1;
	out[gid].y = arg2;
	out[gid].z = arg3;
}

