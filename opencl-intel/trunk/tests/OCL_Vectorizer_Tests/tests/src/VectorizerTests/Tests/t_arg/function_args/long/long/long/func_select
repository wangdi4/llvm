/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL slect function when it recieves long function arguments
 * as input.
 * This function is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_select
( __global const float *in,
		__global float *out,  const long longArg1,  const long longArg2,  const long longArg3)
{
	int gid = get_global_id(0);
	out[gid] = select(longArg1, longArg2, longArg3);
}
