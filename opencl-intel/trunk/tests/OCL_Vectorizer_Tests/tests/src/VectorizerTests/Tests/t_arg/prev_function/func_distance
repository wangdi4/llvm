/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM distance function when it recieves as input double2 produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_distance (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	double2 tmp1 = (double2) (length(in[gid] - 1453.1565), log1p(in[gid]));
	double2 tmp2 = (double2) (in[gid], in[gid] - gid);
	out[gid] = distance(tmp1, tmp2);
}
