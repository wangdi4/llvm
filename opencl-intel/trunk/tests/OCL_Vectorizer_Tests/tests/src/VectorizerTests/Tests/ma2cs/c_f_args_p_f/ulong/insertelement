/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM insertelement instruction when it recieves ulong function argument as input
 * and recieves as input ulong4 argument produced by previous instructions and recieves
 * int const (the position on insretelement).
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
insertelement( __global const float *in,
		__global float8 *out,  const ulong ulongArg1)
{
	int gid = get_global_id(0);
	long x = gid;
	long y = in[gid];
	ulong4 tmp = (ulong4)(x + y, x * y, x - y, x / ( y + x + 17.546464));
	tmp = tmp + tmp;
	tmp.y = ulongArg1;
	out[gid] = as_float8(tmp);
}
