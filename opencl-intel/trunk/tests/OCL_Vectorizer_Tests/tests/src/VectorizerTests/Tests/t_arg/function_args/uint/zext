/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM zext instruction when it recieves uint function argument
 * as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
zext (	__global const float *in,
		__global float *out,  const uint uintArg1)
{
	int gid = get_global_id(0);
	out[gid] = (long) uintArg1;
}
