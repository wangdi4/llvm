/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM alloca instruction when it returns as output uchar4 array and it uses int input produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
alloca_uchar4 (	__global const float *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	uchar4 tmp[10 * 2 + 15];
	tmp[gid * 2] = as_uchar4(in[gid]);
	out[gid] = convert_float4(tmp[gid * 2]);
}
