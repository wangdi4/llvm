/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fast_distance function when it recieves as input float2 produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_fast_distance (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	float2 tmp1 = (float2) (step(in[gid], in[gid] + 10), sign(in[gid]));
	float2 tmp2 = (float2) (in[gid] * gid, in[gid]);
	out[gid] = fast_distance(tmp1, tmp2);
}
