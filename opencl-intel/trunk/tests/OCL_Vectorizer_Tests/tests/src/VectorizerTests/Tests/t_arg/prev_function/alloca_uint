/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM alloca instruction when it returns as output uint array and it uses int input produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
alloca_uint (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	uint tmp[25 + 5];
	tmp[gid + 4] = in[gid]+1;
	out[gid] = tmp[gid + 4]+2;
}
