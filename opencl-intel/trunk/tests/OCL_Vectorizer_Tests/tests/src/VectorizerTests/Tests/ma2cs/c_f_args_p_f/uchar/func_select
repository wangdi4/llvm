/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL select function when it recieves uchar constant
 * and uchar argument produced by previous instructions and uchar
 * function argument as input.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_select
( __global const float *in,
		__global float *out,  const uchar ucharArg1)
{
	int gid = get_global_id(0);
	uchar tmp = gid;
	out[gid] = select(tmp, UCHAR1, ucharArg1);
}
