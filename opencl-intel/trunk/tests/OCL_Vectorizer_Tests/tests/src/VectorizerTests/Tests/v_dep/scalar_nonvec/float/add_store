/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM NON vectorizable store instruction when it receives as input float
 * arguments produced by previous (vectorizable) add instruction that receives
 * constants and function arguments as input and therfore will have scalar value.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
add_store ( __global const float *in,
		__global float *out,  const float floatArg1)
{
	int gid = get_global_id(0);
	out[gid] = floatArg1 + FLOAT1;
}

