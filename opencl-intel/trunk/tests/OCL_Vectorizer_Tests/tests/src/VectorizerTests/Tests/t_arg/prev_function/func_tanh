/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM tanh function when it receives as input double produced by
 * previous instruction.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_tanh (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	double tmp = in[gid] * gid * 0.164149 / 189;
	tmp = rint(tmp) * tmp - 12; 
	out[gid] = tanh(tmp);
}
