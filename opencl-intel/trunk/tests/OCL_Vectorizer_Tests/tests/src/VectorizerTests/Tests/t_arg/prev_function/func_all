/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM all function when it recieves as input char4 produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_all (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	char4 tmp = (char4) ( convert_char(4 * atan2pi(gid, in[gid])), (char)floor(in[gid]), (char)gid, (char)log2(in[gid] * 10));
	out[gid] = all(tmp);
}
