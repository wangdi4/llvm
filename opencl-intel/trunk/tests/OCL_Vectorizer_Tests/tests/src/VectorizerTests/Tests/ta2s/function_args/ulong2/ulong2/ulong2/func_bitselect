/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM bitselect function when it recieves as input int8 produced by
 * previous instruction and called trwice.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_bitselect ( __global const float *in,
		__global float4 *out,  const ulong2 ulong2Arg1,  const ulong2 ulong2Arg2,  const ulong2 ulong2Arg3)
{
	int gid = get_global_id(0);
	ulong2 res1 = bitselect(ulong2Arg1, ulong2Arg2, ulong2Arg3);
	ulong2 res2 = bitselect(ulong2Arg1, ulong2Arg2, ulong2Arg3);
	ulong4 res;
	res.x = res1.x;
	res.y = res1.y;
	res.z = res2.x;
	res.w = res2.y;
	out[gid] = convert_float4(res);
}
