/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM insertelement instruction when it recieves as input float4 arguments
 * produced by previous instructions.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
insertelement_float4 ( __global const float *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	float x =(float) gid;	
	float y =  in[gid];
	float z = (4+ in[gid]);
	float w = isless(x, in[gid] + z);
	float4 tmp = (float4)(x, y, z, w);
	out[gid] = tmp;
}
