/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM smoothstep function when it recieves double function argument as input
 *  and recieves as input double argument produced by previous instructions.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_smoothstep ( __global const float *in,
		__global float2 *out,  const double doubleArg1)
{
	int gid = get_global_id(0);
	double tmp = in[gid] * gid + (in[gid] / 17);
	out[gid] = as_float2(smoothstep(tmp, doubleArg1, tmp));
}
