/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM vicmp instruction when it recieves ulong16 function argument
 * as input and called trwice.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
vicmp (	__global const float *in,
		__global double8 *out,   ulong8 ulong8Arg1,   ulong8 ulong8Arg2)
{
	int gid = get_global_id(0);
	long8 res1 = (ulong8Arg1 == ulong8Arg2);
	ulong8Arg1.x = ulong8Arg1.x + 1 - (ulong8Arg1.x / ulong8Arg1.x);
	long8 res2 = (ulong8Arg1 == ulong8Arg2);
	out[gid] = convert_double8(res1 + res2);
}
