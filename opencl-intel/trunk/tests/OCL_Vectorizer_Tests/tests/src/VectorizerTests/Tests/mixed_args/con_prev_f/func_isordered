/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL isoedered function when it recieves double constant
 * and double argument produced by previous instructions.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_isordered
( __global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	double tmp = in[gid] - degrees((double)(gid / 7 + 29.46413));
	double res = isordered(DOUBLE1, tmp);
	out[gid] = res;
}
