/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM alloca instruction when it returns as output double8* array and it uses int input produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
alloca_double8_ptr (	__global const double8 *in,
		__global int *out)
{
	
	int gid = get_global_id(0);
    double8* tmp[25];
	tmp[gid + 1] = (double8*)(&in[gid]);
	double8* tmp2 = (double8*) 2;
	out[gid] = tmp2;
}
