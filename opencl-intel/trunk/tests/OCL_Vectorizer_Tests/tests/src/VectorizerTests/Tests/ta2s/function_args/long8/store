/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM store instruction when it recieves long8 function argument
 * as input and called trwice.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
store (	__global const float *in,
		__global long8 *out,  const long8 long8Arg1)
{
	int gid = get_global_id(0);
	out[gid] = long8Arg1;
	// these calculations are done to make sure there will be 2 store commands in the llvm code
	gid = gid + 1;
	gid = gid - (gid / gid);
	out[gid] = long8Arg1;
}
