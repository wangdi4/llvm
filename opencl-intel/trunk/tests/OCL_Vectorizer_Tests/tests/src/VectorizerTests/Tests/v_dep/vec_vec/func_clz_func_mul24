/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM vectorizable mul_hi function when it receives as input 2 int arguments
 * produced by previous vectorizable clz function.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_clz_func_mul24 ( __global const float *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	int inGidInt = in[gid];
	int arg1 = clz(gid);
	int arg2 = clz(inGidInt);
	int arg3 = mul24(arg1, arg2);
	out[gid].x = inGidInt;
	out[gid].y = arg1;
	out[gid].z = arg2;
	out[gid].w = arg3;
}
