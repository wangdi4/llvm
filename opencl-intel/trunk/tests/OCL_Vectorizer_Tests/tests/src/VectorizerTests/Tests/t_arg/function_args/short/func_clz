/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL clz function when it recieves short function argument
 * as input.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_clz
( __global const float *in,
		__global float *out,  const short shortArg1)
{
	int gid = get_global_id(0);
	out[gid] = clz(shortArg1);
}
