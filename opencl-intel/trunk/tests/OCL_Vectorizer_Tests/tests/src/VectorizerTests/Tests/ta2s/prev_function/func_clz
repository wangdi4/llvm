/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM clz function when it recieves as input long produced by
 * previous instruction and called twice.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_clz (	__global const int *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	long tmp = sub_sat(gid * isfinite( (float) in[gid]), in[gid]);
	long res1 = clz(tmp);
	long res2 = clz(tmp);
	long2 res;
	res.x = res1;
	res.y = res2;
	out[gid] = as_float4(res);
}
