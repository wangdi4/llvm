/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM icmp instruction when it recieves ushort constant and recieves
 * ushort function argument as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
icmp ( __global const float *in,
		__global float *out,  const ushort ushortArg1)
{
	int gid = get_global_id(0);
	out[gid] = convert_float(USHORT1 == ushortArg1);
}
