/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM getelementptr instruction when it recieves char* and int function argument
 * as input that was created by sub.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
getelementptr_sub (	__global const char *in,
		__global float *out,  const int intArg1)
{
	int gid = get_global_id(0);
	char* tmp1 = (char*) in;
	char* tmp2 = tmp1 + gid;
	char* tmp3 = tmp2 - intArg1;
	// select added to make sure not going over the array range
	char* tmp4 = tmp3 < tmp1 ? tmp1 : tmp3;
	char* tmp5 = tmp4 > tmp2 ? tmp2 : tmp4;
	out[gid] = *tmp5;
}
