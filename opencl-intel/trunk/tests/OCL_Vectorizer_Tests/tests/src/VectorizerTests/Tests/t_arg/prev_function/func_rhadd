/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM rhad function when it recieves as input char produced by
 * previous instruction.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_rhadd (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	char tmp1 = convert_char(in[gid] * exp(in[gid]));
	char tmp2 = (char)(in[gid] * expm1(in[gid]));
	out[gid] = rhadd(tmp1, tmp2);
}
