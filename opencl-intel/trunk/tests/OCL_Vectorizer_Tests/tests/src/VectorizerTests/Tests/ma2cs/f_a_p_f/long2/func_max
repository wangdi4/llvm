/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM max function when it recieves long2 argument produced by previous instructions
 * and recieves long2 function argument as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_max ( __global const float *in,
		__global float4 *out,  const long2 long2Arg1)
{
	int gid = get_global_id(0);
	long2 tmp = (long2)(gid * gid * gid * in[gid], in[gid] * in[gid] * in[gid] *gid);
	out[gid] = as_float4(max(long2Arg1, tmp));
}
