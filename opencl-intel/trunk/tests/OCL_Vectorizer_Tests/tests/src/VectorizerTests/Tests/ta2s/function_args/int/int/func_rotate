/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL rootn function when it recieves int function arguments
 * as input and called twice.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_rotate ( __global const float *in,
		__global float2 *out,  const int intArg1,  const int intArg2)
{
	int gid = get_global_id(0);
	float res1 = rotate(intArg1, intArg2);
	float res2 = rotate(intArg1, intArg2);
	out[gid].x = res1;
	out[gid].y = res2;
}
