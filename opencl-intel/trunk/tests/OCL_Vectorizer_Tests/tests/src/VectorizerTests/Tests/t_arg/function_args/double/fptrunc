/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fptrunc instruction when it recieves double function argument
 * as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fptrunc (	__global const float *in,
		__global float *out,  const double doubleArg1)
{
	int gid = get_global_id(0);
	// storing double into float will cause fptrunc
	out[gid] = doubleArg1;
}
