/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM select instruction when it recieves int function arguments.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
select_int ( __global const float2 *in,
		__global float *out,  const int intArg1, const int intArg2, const int intArg3)
{
	int gid = get_global_id(0);
	out[gid] = intArg1 ? intArg2 : intArg3;
}
