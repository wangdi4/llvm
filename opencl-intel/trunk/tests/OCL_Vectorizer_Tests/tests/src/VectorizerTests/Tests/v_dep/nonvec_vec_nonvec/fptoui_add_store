/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM non vectorizable store instruction appearing after the vectorizable udiv instruction when
 * they both receive as input ushort arguments produced by previous NON vectorizable
 * fptoui instruction.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
fptoui_add_store ( __global const float *in,
		__global ushort *out)
{
	int gid = get_global_id(0);
	ushort arg1 = in[gid];
	ushort arg2 = ((float) gid * 5.1654);
	out[gid] = arg1 + arg2;


}
