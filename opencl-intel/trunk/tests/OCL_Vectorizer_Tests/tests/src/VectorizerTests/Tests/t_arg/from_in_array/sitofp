/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fptosi instruction when it recieves short function argument
 * as input and turns it into float.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
sitofp (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	short tmp = ARG0;
	// this add is done to make sure that fptosi double to long is used on tmp
	tmp = tmp + 3;
	out[gid] = tmp;
}
