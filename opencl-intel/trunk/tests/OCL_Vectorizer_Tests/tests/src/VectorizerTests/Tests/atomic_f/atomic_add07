__kernel void
atomic_add07
( volatile __global  int *in,
		__global float *out)
{
	int gid = get_global_id(0);
	in[gid]=gid;
	
	int rez=atomic_add(&in[gid],gid)+atomic_add(&in[gid],gid);
	
	
	out[gid] = rez+in[gid];
}
