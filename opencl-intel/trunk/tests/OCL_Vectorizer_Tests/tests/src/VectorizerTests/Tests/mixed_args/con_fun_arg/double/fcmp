/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM fcmp instruction when it recieves double constant and recieves
 * double function argument as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
fcmp ( __global const float *in,
		__global float *out,  const double doubleArg1)
{
	int gid = get_global_id(0);
	out[gid] = convert_float(DOUBLE1 == doubleArg1);
}
