/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM alloca instruction when it returns as output ulong2 array and it uses int input produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
alloca_ulong2 (	__global const float2 *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	ulong2 tmp[(100 + 14) * 3];
	tmp[gid + 10] = convert_ulong2(in[gid]);
	out[gid] = as_float4(tmp[gid + 10]);
}
