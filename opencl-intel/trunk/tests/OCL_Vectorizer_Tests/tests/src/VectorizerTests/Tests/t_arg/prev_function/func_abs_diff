/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM abs_diff function when it recieves as input ulong produced by
 * previous instruction.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_abs_diff (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	ulong tmp1 = upsample( (int)floor(in[gid] * 7 / 3), 8 * gid);
	ulong tmp2 = convert_ulong(gid);
	out[gid] = abs_diff(tmp1, tmp2);
}
