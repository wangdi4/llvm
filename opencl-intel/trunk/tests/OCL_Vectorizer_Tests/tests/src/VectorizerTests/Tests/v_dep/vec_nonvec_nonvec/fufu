/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM NON vectorizable all function appearing after the NON vectorizable any function when
 * they both receive as input ulong arguments produced by previous vectorizable
 * add_sat function.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *func_add_sat_func_any_func_all
 */
__kernel void
fufu
 ( __global const float *in,
		__global float2 *out)
{
	int gid = get_global_id(0);
	long arg1 = gid;
	long arg2 = in[gid];
	long arg3 = add_sat(arg1, arg2);
	int arg4 = any(arg3);
	int arg5 = all(arg3);
	out[gid].x = arg4;
	out[gid].y = arg5;
}
