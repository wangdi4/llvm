/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM vicmp instruction when it recieves char2 constant
 * and char2 function argument as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
vicmp ( __global const float *in,
		__global float2 *out,  const char2 char2Arg1)
{
	int gid = get_global_id(0);
	out[gid] = convert_float2(CHAR2_VEC1 == char2Arg1);
}
