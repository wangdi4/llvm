/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM sdiv instruction when it recieves long function arguments
 * as input.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
sdiv (	__global const float *in,
		__global float *out,  const long longArg1,  const long longArg2)
{
	int gid = get_global_id(0);
	out[gid] = longArg1 / longArg2;
}
