/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM ashr instruction when it recieves int function arguments
 * as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
ashr (	__global const float *in,
		__global float *out,  const int intArg1,  const int intArg2)
{
	int gid = get_global_id(0);
	long tmp = intArg1 >> intArg2;
	// added 1 to make sure that a variable is created llvm for tmp
	// and so that sext will be used to create that variable
	out[gid] = tmp + 1;
}
