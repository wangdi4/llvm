/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM udiv instruction when it recieves uint global.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
udiv (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	out[gid] = ((uint) ARG2) / ((uint) ARG1);
}
