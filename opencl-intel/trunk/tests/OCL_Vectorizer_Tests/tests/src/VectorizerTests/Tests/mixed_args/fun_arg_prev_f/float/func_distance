/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM distance function when it recieves float function argument as input
 *  and recieves as input float argument produced by previous instructions.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_distance ( __global const float *in,
		__global float *out,  const float floatArg1)
{
	int gid = get_global_id(0);
	float tmp = in[gid] - gid * gid;
	out[gid] = distance(floatArg1, tmp);
}
