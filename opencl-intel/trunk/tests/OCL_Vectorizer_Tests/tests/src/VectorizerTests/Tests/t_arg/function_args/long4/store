/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM store instruction when it recieves long4 function argument
 * as input.
 * This instruction is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
store (	__global const float *in,
		__global long4 *out,  const long4 long4Arg1)
{
	int gid = get_global_id(0);
	out[gid] = long4Arg1;
}
