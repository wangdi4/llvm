/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the OpenCL exp10 function when it receives float function argument
 * as input.
 * This function is vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
func_exp10
( __global const float *in,
		__global float *out,  const double doubleArg1)
{
	int gid = get_global_id(0);
	out[gid] = exp10(doubleArg1);
}
