/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM udiv instruction when it recieves ushort2 constant
 * and called trwice.
 * This instruction is vectorizable and called twice.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */

__kernel void
udiv ( __global const float *in,
		__global float4 *out)
{
	int gid = get_global_id(0);
	ushort2 vec = (ushort2)(ARG1, ARG1);
	ushort2 res1 = USHORT2_VEC1 / vec;
	vec = vec + vec;
	ushort2 res2 = USHORT2_VEC1 / vec;
	out[gid].x = res1.x;
	out[gid].y = res1.y;
	out[gid].z = res2.x;
	out[gid].w = res2.y;
}
