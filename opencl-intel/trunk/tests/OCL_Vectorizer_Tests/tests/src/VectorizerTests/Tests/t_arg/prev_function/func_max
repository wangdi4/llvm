/*
 * Part of the tests that check different LLVM instructions on different argument
 * types and different argument sources.
 *
 * Tests the LLVM max function when it recieves as input short produced by
 * previous instruction.
 * This instruction is NOT vectorizable.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
func_max (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	float tmp = (float)(max(gid, (int)ceil(gid * 0.7)));
	out[gid] = max(in[gid], in[(int)tmp]) * tmp;
}
