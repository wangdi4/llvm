/*
 * Part of the general tests that check different kernels.
 *
 * Tests the LLVM mul instruction when it recieves float constants.
 *
 * @param in
 * 		The input array.
 * @param out
 * 		The output array.
 *
 */
__kernel void
const_mul (	__global const float *in,
		__global float *out)
{
	int gid = get_global_id(0);
	out[gid] = FLOAT1 * FLOAT2;
}
