; Test whether getGroups() group these two memrefs correctly.
; Expected output: There should be two groups generated by the grouper
; since the group size is 16 bytes and the memrefs are 64 bytes apart from each other.
; Therefore, Group#1 will contain (1) Group#2 will contain (2)
; Unexpected output: Group#1 will contain both memrefs(1, 2)
; REQUIRES: asserts
; RUN: intelovls-test < %s -debug 2>&1 | FileCheck %s
;
; CHECK: Group#
; CHECK: Group#
; CHECK-NOT: Group#
# 16
1 A 0 i32 4 SLoad C 40
2 A 64 i32 4 SLoad C 40