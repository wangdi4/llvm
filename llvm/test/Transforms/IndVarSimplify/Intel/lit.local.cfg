import lit.llvm.util

# Default lit.cfg.py added default options to revert to llorg behavior.
# In Intel specific folder, we want to remove them so that we can test the Intel specific opts.
lit.llvm.util.remove_default_options_from_tool(config, 'opt', config.opt_options_to_mimic_llorg)
lit.llvm.util.remove_default_options_from_tool(config, 'llc', config.llc_options_to_mimic_llorg)
