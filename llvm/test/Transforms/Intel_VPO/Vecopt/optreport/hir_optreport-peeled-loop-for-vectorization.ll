; NOTE: Assertions have been autogenerated by utils/intel_update_vplan_checks.py
; RUN: opt -passes="hir-ssa-deconstruction,hir-temp-cleanup,hir-last-value-computation,hir-vec-dir-insert,hir-vplan-vec,hir-cg,simplifycfg,intel-ir-optreport-emitter" -disable-output -vplan-force-vf=4 -intel-opt-report=high < %s 2>&1 | FileCheck %s

; HIR before VPlan-
;  BEGIN REGION { modified }
;        + DO i1 = 0, %n + -1, 1   <DO_MULTI_EXIT_LOOP>
;        |   if ((%t4)[i1] == &((%t1)[0]))
;        |   {
;        |      %gep = &((%t4)[i1]);
;        |      goto found;
;        |   }
;        + END LOOP
;  END REGION

target datalayout = "e-m:e-i64:64-f80:128-n8:16:32:64-S128"
target triple = "x86_64-unknown-linux-gnu"

%struct2 = type <{ i32, [4 x i8] }>

; Function Attrs: uwtable
define ptr @foo(ptr %t4, ptr %t1, i64 %n) {
; CHECK-LABEL:  Global optimization report for : foo
; CHECK-EMPTY:
; CHECK-NEXT:  LOOP BEGIN
; CHECK-NEXT:  <Peeled loop for vectorization>
; CHECK-NEXT:  LOOP END
; CHECK:  LOOP BEGIN
; CHECK:  <Remainder loop for vectorization>
; CHECK-NEXT:      remark #15441: remainder loop was not vectorized:
; CHECK-NEXT:  LOOP END
; CHECK-NEXT:  =================================================================
;
entry:
  br label %header

header:
  %iv = phi i64 [0, %entry], [%iv.next, %latch]
  %gep = getelementptr ptr, ptr %t4, i64 %iv
  %ptr = load ptr, ptr %gep
  %cmp.found = icmp eq ptr %ptr, %t1
  br i1 %cmp.found, label %found, label %latch

latch:
  %iv.next = add i64 %iv, 1
  %exitcond = icmp eq i64 %iv.next, %n
  br i1 %exitcond, label %loop.exit, label %header

loop.exit:
  br label %exit

found:
  %lcssa = phi ptr [ %gep , %header ]
  br label %exit

exit:
  %val = phi ptr [ %lcssa, %found ], [ zeroinitializer, %loop.exit ]
  ret ptr %val
}
