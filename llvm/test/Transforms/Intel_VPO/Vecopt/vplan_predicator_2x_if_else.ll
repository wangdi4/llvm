; NOTE: Assertions have been autogenerated by utils/intel_update_vplan_checks.py
; RUN: opt %s -VPlanDriver -enable-new-vplan-predicator=false -vplan-predicator-report -disable-predicator-opts -disable-vplan-codegen -disable-vplan-subregions=0 -S -o /dev/null | FileCheck %s -check-prefix=NOOPT
; RUN: opt %s -VPlanDriver -enable-new-vplan-predicator=false -vplan-predicator-report -disable-vplan-codegen -disable-vplan-subregions=0 -S -o /dev/null | FileCheck %s -check-prefix=OPT
; Verify VPlan predicator: two nested if-else statements.

; region1
; -------
; BB11
;  |
;  v
; loop16
;  |
;  v
; BB12


; loop16
; ------
; BB10
;  |
;  v
; BB2<-----+
;  |       |
;  v       |
; region17 |
;  |       |
;  v       |
; region18 |
;  |       |
;  v       |
; BB15-----+
;  |
;  v
; BB9


; region18
; --------
; BB14
;  | \
;  v  v
; BB6 BB7
;  |  /
;  v v
; BB8


; region17
; --------
; BB13
;  |  \
;  v   v
; BB3 BB4
;  |  /
;  v v
; BB5


; 1. icx test.c -o test_noopt.ll -fopenmp -Qoption,c,-fintel-openmp -O0 -restrict -S -emit-llvm
; 2. opt test_noopt.ll -O2 -debug-pass=Arguments
; 3. opt test_noopt.ll -S -o pred_if_else.ll -loopopt=false (+ all the flags from -O2 from #2, but -VPODirectiveCleanup and -loop-unroll)


; void foo(int * restrict a, int * restrict b, int * restrict c, int N)
; {
;   int i;
; #pragma omp simd
;   for (i = 0; i < 300; i++) {
;     if (a[i] > 0)
;       b[i] = b[i] * 5;
;     else
;       a[i] = a[i] + 5;
;
;     c[i] = c[i] * N;
;
;     if (c[i] > 0)
;       a[i] = c[i] - a[i];
;     else
;       b[i] = a[i] * c[i];
;   }
; }

; ModuleID = 'pred_2x_if_else_noopt.ll'
source_filename = "pred_2x_if_else.c"
target datalayout = "e-m:e-i64:64-f80:128-n8:16:32:64-S128"
target triple = "x86_64-unknown-linux-gnu"

; Function Attrs: noinline nounwind uwtable
define void @foo(i32* noalias nocapture %a, i32* noalias nocapture %b, i32* noalias nocapture %c, i32 %N) local_unnamed_addr #0 {
; NOOPT-LABEL:  Predicator report
; NOOPT-NEXT:  [[LOOP0:loop[0-9]+]]:
; NOOPT-NEXT:    [[BB0:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP0:BP[0-9]+]] =
; NOOPT-NEXT:    [[BB1:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP1:BP[0-9]+]] = [[BP0]]
; NOOPT-NEXT:    [[REGION0:region[0-9]+]]:
; NOOPT-NEXT:      [[BP1]] = [[BP0]]
; NOOPT-NEXT:    [[REGION1:region[0-9]+]]:
; NOOPT-NEXT:      [[BP1]] = [[BP0]]
; NOOPT-NEXT:    [[BB2:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP2:BP[0-9]+]] = [[BP1]]
; NOOPT-NEXT:    [[BB3:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP3:BP[0-9]+]] = [[BP0]]
; NOOPT-NEXT:  End of the Predicator report.
; NOOPT-NEXT:  Predicator report
; NOOPT-NEXT:  [[REGION0]]:
; NOOPT-NEXT:    [[BB4:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP4:BP[0-9]+]] = [[BP1]]
; NOOPT-NEXT:      [[IFF0:IfF[0-9]+]] = [[BP4]] && !i1 [[VP_CMP1:%.*]]
; NOOPT-NEXT:      [[IFT0:IfT[0-9]+]] = [[BP4]] && i1 [[VP_CMP1]]
; NOOPT-NEXT:    [[BB5:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP5:BP[0-9]+]] = [[IFF0]]
; NOOPT-NEXT:    [[BB6:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP6:BP[0-9]+]] = [[IFT0]]
; NOOPT-NEXT:    [[BB7:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP7:BP[0-9]+]] = [[BP5]] || [[BP6]]
; NOOPT-NEXT:  End of the Predicator report.
; NOOPT-NEXT:  Predicator report
; NOOPT-NEXT:  [[REGION1]]:
; NOOPT-NEXT:    [[BB8:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP8:BP[0-9]+]] = [[BP1]]
; NOOPT-NEXT:      [[IFF1:IfF[0-9]+]] = [[BP8]] && !i1 [[VP_CMP17:%.*]]
; NOOPT-NEXT:      [[IFT1:IfT[0-9]+]] = [[BP8]] && i1 [[VP_CMP17]]
; NOOPT-NEXT:    [[BB9:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP9:BP[0-9]+]] = [[IFF1]]
; NOOPT-NEXT:    [[BB10:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP10:BP[0-9]+]] = [[IFT1]]
; NOOPT-NEXT:    [[BB11:BB[0-9]+]]:
; NOOPT-NEXT:      [[BP11:BP[0-9]+]] = [[BP9]] || [[BP10]]
; NOOPT-NEXT:  End of the Predicator report.
;
; OPT-LABEL:  Predicator report
; OPT-NEXT:  [[REGION0:region[0-9]+]]:
; OPT-NEXT:    [[BB0:BB[0-9]+]]:
; OPT-NEXT:      [[IFF0:IfF[0-9]+]] = !i1 [[VP_CMP1:%.*]]
; OPT-NEXT:      [[IFT0:IfT[0-9]+]] = i1 [[VP_CMP1]]
; OPT-NEXT:    [[BB1:BB[0-9]+]]:
; OPT-NEXT:      [[BP0:BP[0-9]+]] = [[IFF0]]
; OPT-NEXT:    [[BB2:BB[0-9]+]]:
; OPT-NEXT:      [[BP1:BP[0-9]+]] = [[IFT0]]
; OPT-NEXT:    [[BB3:BB[0-9]+]]:
; OPT-NEXT:  End of the Predicator report.
; OPT-NEXT:  Predicator report
; OPT-NEXT:  [[REGION1:region[0-9]+]]:
; OPT-NEXT:    [[BB4:BB[0-9]+]]:
; OPT-NEXT:      [[IFF1:IfF[0-9]+]] = !i1 [[VP_CMP17:%.*]]
; OPT-NEXT:      [[IFT1:IfT[0-9]+]] = i1 [[VP_CMP17]]
; OPT-NEXT:    [[BB5:BB[0-9]+]]:
; OPT-NEXT:      [[BP2:BP[0-9]+]] = [[IFF1]]
; OPT-NEXT:    [[BB6:BB[0-9]+]]:
; OPT-NEXT:      [[BP3:BP[0-9]+]] = [[IFT1]]
; OPT-NEXT:    [[BB7:BB[0-9]+]]:
; OPT-NEXT:  End of the Predicator report.
; OPT-NEXT:  Predicator report
; OPT-NEXT:  [[LOOP0:loop[0-9]+]]:
; OPT-NEXT:    [[BB8:BB[0-9]+]]:
; OPT-NEXT:    [[BB9:BB[0-9]+]]:
; OPT-NEXT:    [[REGION0]]:
; OPT-NEXT:    [[REGION1]]:
; OPT-NEXT:    [[BB10:BB[0-9]+]]:
; OPT-NEXT:    [[BB11:BB[0-9]+]]:
; OPT-NEXT:  End of the Predicator report.
;
entry:
  %tok = call token @llvm.directive.region.entry() [ "DIR.OMP.SIMD"() ]
  br label %for.body

for.body:                                         ; preds = %for.inc, %entry
  %indvars.iv = phi i64 [ 0, %entry ], [ %indvars.iv.next, %for.inc ]
  %arrayidx = getelementptr inbounds i32, i32* %a, i64 %indvars.iv
  %0 = load i32, i32* %arrayidx, align 4
  %cmp1 = icmp sgt i32 %0, 0
  br i1 %cmp1, label %if.then, label %if.else

if.then:                                          ; preds = %for.body
  %arrayidx3 = getelementptr inbounds i32, i32* %b, i64 %indvars.iv
  %1 = load i32, i32* %arrayidx3, align 4
  %mul = mul nsw i32 %1, 5
  store i32 %mul, i32* %arrayidx3, align 4
  br label %if.end

if.else:                                          ; preds = %for.body
  %add = add nsw i32 %0, 5
  store i32 %add, i32* %arrayidx, align 4
  br label %if.end

if.end:                                           ; preds = %if.else, %if.then
  %2 = phi i32 [ %add, %if.else ], [ %0, %if.then ]
  %arrayidx11 = getelementptr inbounds i32, i32* %c, i64 %indvars.iv
  %3 = load i32, i32* %arrayidx11, align 4
  %mul12 = mul nsw i32 %3, %N
  store i32 %mul12, i32* %arrayidx11, align 4
  %cmp17 = icmp sgt i32 %mul12, 0
  br i1 %cmp17, label %if.then18, label %if.else25

if.then18:                                        ; preds = %if.end
  %sub = sub nsw i32 %mul12, %2
  store i32 %sub, i32* %arrayidx, align 4
  br label %for.inc

if.else25:                                        ; preds = %if.end
  %mul30 = mul nsw i32 %2, %mul12
  %arrayidx32 = getelementptr inbounds i32, i32* %b, i64 %indvars.iv
  store i32 %mul30, i32* %arrayidx32, align 4
  br label %for.inc

for.inc:                                          ; preds = %if.then18, %if.else25
  %indvars.iv.next = add nuw nsw i64 %indvars.iv, 1
  %exitcond = icmp eq i64 %indvars.iv.next, 300
  br i1 %exitcond, label %for.end, label %for.body

for.end:                                          ; preds = %for.inc
  call void @llvm.directive.region.exit(token %tok) [ "DIR.OMP.END.SIMD"()]
  ret void
}

; Function Attrs: nounwind
declare token @llvm.directive.region.entry() #1

; Function Attrs: nounwind
declare void @llvm.directive.region.exit(token) #1

attributes #0 = { noinline nounwind uwtable "disable-tail-calls"="false" "less-precise-fpmad"="false" "no-frame-pointer-elim"="true" "no-frame-pointer-elim-non-leaf" "no-infs-fp-math"="false" "no-jump-tables"="false" "no-nans-fp-math"="false" "stack-protector-buffer-size"="8" "target-cpu"="x86-64" "target-features"="+fxsr,+mmx,+sse,+sse2,+x87" "unsafe-fp-math"="false" "use-soft-float"="false" }
attributes #1 = { argmemonly nounwind }
