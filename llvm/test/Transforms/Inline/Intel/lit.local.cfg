import lit.llvm.util

# Default lit.cfg.py added default options to revert to llorg behavior.
# In Intel specific folder, we want to remove them so that we can test the Intel specific opts.
lit.llvm.util.remove_default_options_from_tool(config, 'opt', config.options_to_revert_to_llorg_behavior)
lit.llvm.util.remove_default_options_from_tool(config, 'llc', config.options_to_revert_to_llorg_behavior)
