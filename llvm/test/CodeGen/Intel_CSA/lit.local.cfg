import re

config.suffixes = ['.ll']

if not 'CSA' in config.root.targets:
    config.unsupported = True

# For now we don't test csa-win32.
if re.search(r'cygwin|mingw32|win32', config.target_triple):
    config.unsupported = True
CSABASE_ROOT = os.environ.get('CSABASE_ROOT', '')
config.substitutions.append(('%CSABASE_ROOT', CSABASE_ROOT))
