; RUN: FileCheck -check-prefix=A -input-file %s %s
; RUN: FileCheck -check-prefix=B -input-file %s %s

this is the string to be matched ;INTEL

; should match with or without ;INTEL at the end
; This also assures the ;INTEL isn't stripped out of a reg-ex.
;
; A-DAG: this is the string to be {{matched ;INTEL}}
; B-DAG: this is the string to be {{matched ;INTEL}}     ;INTEL

