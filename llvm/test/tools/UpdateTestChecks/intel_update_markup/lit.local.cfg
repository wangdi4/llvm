# These tests are for llc/opt.
if 'llc-binary' not in config.available_features and 'opt-binary' not in config.available_features:
    config.unsupported = True
else:
    config.environment.update(os.environ)  # for git & sed
